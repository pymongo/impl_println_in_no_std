// #![feature(once_cell)]
#![no_std]
// use core::ptr::null;
// use core::lazy::Lazy;

// struct String;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
